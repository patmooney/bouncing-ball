### BouncingBall
A pure JS loading animation

``` javascript
    var myBalls = new BouncingBall(
        document.getElementById('container'),
        {
            numberOfBalls: 3,
            ballColour: "pink",
            ballRadius: 10
        }
    );
```
